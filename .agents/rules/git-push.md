# Git Push Policy

- NEVER run `git push` under any circumstances unless the USER explicitly instructs or gives permission in their prompt to push.
- Keep all changes, builds, and tests strictly local until explicit user approval is granted.
